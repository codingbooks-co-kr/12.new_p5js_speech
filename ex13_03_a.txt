// 아두이노: 수신된 1바이트의 정수(→0∼255)로 LED 밝기 제어

#define LED 6

void setup() {
  pinMode(LED, OUTPUT);
  Serial.begin(9600);
}

void loop() { }

void serialEvent() {		// 시리얼 버퍼에 값이 수신되면 자동 호출
  int value = Serial.read();	// 수신 데이터 1바이트 읽기
  analogWrite(LED, value);	// 수신 데이터(→0∼255)로 LED 밝기 제어
}