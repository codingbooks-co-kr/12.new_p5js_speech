// 아두이노: 가변저항값 송신2

#define VARIABLE_R A1

void setup() {
  pinMode(VARIABLE_R, INPUT);
  Serial.begin(9600);
}

void loop( ) {
  int value = analogRead(VARIABLE_R);	// 가변저항값(0∼1023) 읽기
  Serial.println(value);				// 가변저항값(0∼1023)을 문자열로 송신
  delay(30);					// 전송 속도 지연
}